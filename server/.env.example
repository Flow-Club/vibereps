# VibeReps Server Configuration

# Database URL (SQLite default, or use PostgreSQL for production)
DATABASE_URL=sqlite:///./vibereps.db
# DATABASE_URL=postgresql://user:password@localhost/vibereps

# Server host and port
HOST=0.0.0.0
PORT=8000
